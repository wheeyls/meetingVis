extends layout

block content
  h2 Creating
    .meeting-title

  #the-app
    #the-time
    ul(id='the-list')
    input(id='user-name-input')
    a(href='#', id='add-user') Click to Add Attendee/Topic

    .finished 
      a#end-meeting(href='#') Click to end the meeting
    .finished
      .meeting-text
      #show-meeting

  script#user-template(type='text/template')
    <div class='username'><%= name %>
    <span class='small'>(Click
    <% if (choice) { %>
    | or Ctrl-<%= choice %>
    <% } %>)</span>
    </div>
    <div class='timeline'></div>
