extends layout

block content
  h2 Creating
    .meeting-title

  #the-app
    ul(id='the-list')
    input(id='user-name-input')
    a(href='#', id='add-user') Click to Add User

    .finished 
      a#end-meeting(href='#') Click to end the meeting
    .finished
      .meeting-text
      #show-meeting

  script#user-template(type='text/template')
    <div class='username'>(Ctrl <%= choice %>) <%= name %></div>
    <div class='timeline'></div>
